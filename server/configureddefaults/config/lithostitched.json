{
  // If disabled, some mod compat features will be turned off to prioritize parity with vanilla seeds.
  // The following features will break if disabled:
  // - Custom wood type shipwrecks
  // - Structure optimizations
  // true = Lithostitched jigsaw optimizations + mod structure compat (layouts differ from vanilla; structure locations unchanged).
  // false = vanilla seed-parity layouts; some modded village pieces may fail to generate (see Lithostitched #41).
  "breaks_seed_parity": true,
  "log_debug_messages": false
}